# Scout Analytics v3.1.1-minimal-stable Fallback Configuration

# Build Mode
NODE_ENV=production
NEXT_PUBLIC_ENVIRONMENT=fallback-minimal
NEXT_PUBLIC_VERSION=3.1.1-minimal-stable

# Feature Flags - DISABLE ALL EXTERNAL DEPENDENCIES
SKIP_DATA=true
MINIMAL_UI=true
DISABLE_AI_AGENTS=true
DISABLE_SUPABASE=true
DISABLE_EXTERNAL_APIS=true
SAFE_MODE=true

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://scout-mvp.vercel.app
NEXT_PUBLIC_PLATFORM_NAME=Scout Analytics (Minimal Mode)
NEXT_PUBLIC_FALLBACK_MODE=true

# Stubbed Services (No real values needed)
DATABASE_URL=postgresql://localhost:5432/stub
SUPABASE_URL=https://stub.supabase.co
SUPABASE_ANON_KEY=stub_key
OPENAI_API_KEY=stub_key
VERCEL_TOKEN=stub_token

# Build Settings
NEXT_TELEMETRY_DISABLED=1
TURBO_TELEMETRY_DISABLED=1

# QA Mode
QA_MODE=safe
AUDIT_LEVEL=structural_only
SKIP_INTEGRATION_TESTS=true